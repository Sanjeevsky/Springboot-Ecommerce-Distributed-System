// Trove — design tokens surfaced to JS (for inline styles where a CSS var
// isn't convenient). Keep in sync with src/styles/*.css.
export const font = {
  display: "var(--font-display)",
  text: "var(--font-text)",
  mono: "var(--font-mono)",
};
export const radius = {
  sm: "var(--radius-sm)",
  md: "var(--radius-md)",
  lg: "var(--radius-lg)",
  xl: "var(--radius-xl)",
  pill: "var(--radius-pill)",
};
