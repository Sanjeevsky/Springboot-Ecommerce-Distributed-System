# Local Docker Config Server placeholder.
# Application containers use packaged service properties; this native repo keeps
# cloud-config healthy without depending on a remote Git clone during local runs.
configserver.local.mode=true
